services:
  frontend:
    build:
      context: ./frontend
    container_name: aihexa-frontend-compose
    ports:
      - "8080:80"

    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s